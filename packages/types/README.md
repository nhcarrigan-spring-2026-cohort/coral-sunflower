# @repo/types

## Generate types from supabase

Run this command in the src directory.

Run it every time you make changes to the database.

```bash
npx supabase gen types typescript --local > database.types.ts
```
